# Reporch CLI native package

This package contains one platform-specific binary used by `@reporch/cli`.
Install `@reporch/cli` instead of depending on this package directly.

The package has no lifecycle scripts and performs no network access during
installation. Source, build instructions, checksums, SBOMs, and provenance are
available at https://github.com/Reporch/cli.
